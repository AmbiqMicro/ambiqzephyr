description: Ambiq sdmmc disk access

compatible: "ambiq,sdio"

include: [mmc.yaml, pinctrl-device.yaml, reset-device.yaml]

properties:

  bus-width:
    type: int
    default: 4
    description: |
        bus width for SDMMC access
    enum:
      - 1
      - 4
      - 8


