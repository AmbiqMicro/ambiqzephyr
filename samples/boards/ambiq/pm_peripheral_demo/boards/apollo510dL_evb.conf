# The board chooses an enabled crc32 node and the SoC is APOLLO5X, so the
# Ambiq hardware CRC driver can be built. CRC_DRIVER is a menuconfig that
# defaults to n, so enable it here to exercise the CRC32 demo thread.
CONFIG_CRC_DRIVER=y
