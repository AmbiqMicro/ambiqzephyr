sample:
  name: Apollo510 EB PSRAM SPI
tests:
  sample.boards.apollo510_eb.spi_serial_flash:
    tags:
      - LED
      - gpio
    filter: dt_enabled_alias_with_parent_compat("led0", "gpio-leds")
    depends_on: gpio
    harness: led
    integration_platforms:
      - apollo510_eb
