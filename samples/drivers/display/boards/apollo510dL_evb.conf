CONFIG_HEAP_MEM_POOL_SIZE=831744
CONFIG_DCACHE=n

# Verbose logs for Ambiq DC SPI (spi_ambiq_dcif) and CO5300 (requires CONFIG_LOG in prj.conf)
CONFIG_SPI_LOG_LEVEL_DBG=y
CONFIG_DISPLAY_LOG_LEVEL_DBG=y
